const { expect } = require('chai');

describe('Express server', () => {

  // beforeEach() - this callback runs once before _every_ test

  it('works at all', () => {
    console.log('The test is running!');
  });

});
